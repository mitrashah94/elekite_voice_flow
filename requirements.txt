# VoiceFlow — Dependencies
# Install: pip install -r requirements.txt

# Core
openai>=1.12.0
sounddevice>=0.4.6
numpy>=1.24.0
pynput>=1.7.6

# macOS menu bar (optional — falls back to CLI mode without it)
rumps>=0.4.0
