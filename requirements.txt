# VoiceFlow — Dependencies
# Install: pip install -r requirements.txt

# Core
openai>=1.12.0
sounddevice>=0.4.6
numpy>=1.24.0
pynput>=1.7.6

# Windows clipboard (cross-platform, used by Windows backend)
pyperclip>=1.8.0

# macOS menu bar (optional — falls back to CLI mode without it)
rumps>=0.4.0

# Windows system tray (optional — falls back to CLI mode without it)
pystray>=0.19.0
Pillow>=9.0.0

# Windows toast notifications (optional — falls back to logging without it)
winotify>=1.1.0; sys_platform == "win32"
